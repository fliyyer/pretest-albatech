export interface CardPortofolioProps {
    image: string;
    category: string;
    title: string;
    description: string;
}

export interface CardServicesProps {
    image: string;
    title: string;
    description: string;
}

export interface CardTestimonialProps {
    image: string;
    title: string;
    feedback: string;
    name: string;
    position: string;
}